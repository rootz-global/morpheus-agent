{
  "name": "@rootz/morpheus-agent",
  "version": "0.2.0",
  "description": "Morpheus AI agent as a Rootz data wallet — birth certificate, action logging, code delivery, wallet-watch",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "rootz-morpheus": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "@rootz/chain-reader": "*",
    "@rootz/crypto": "*",
    "@rootz/integration": "*",
    "@rootz/notes": "*",
    "@rootz/tee-policy": "*",
    "ethers": "^6.9.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
